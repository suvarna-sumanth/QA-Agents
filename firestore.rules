rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    /**
     * PROTOTYPE RULES
     * Allows all authenticated users full access to simplify the development loop.
     */
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}